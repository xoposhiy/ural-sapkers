Программа, решающая вторую задачу

: INIT 'CUR' VAR 0 ->CUR 'STR' VAR ->STR 'ALPHA' VAR ->ALPHA //;
: MAJEDOC INIT REC . // ;
: REC @STR LENGTH 0 == IFTHEN @CUR ELSE @ALPHA @STR 0 CHARAT POS @STR LENGTH 1 - @ALPHA LENGTH * 1 + * @CUR + ->CUR @STR 1 @STR LENGTH SUBSTR ->STR 
REC ENDIF //;
